Based on the task context files, including `config/config.yaml` and task logic files (`main.py`, `run_trial.py`, and, if present, `util.py` or `utils.py`), generate `README.md`.

- `main.py` defines setup and block-level flow.
- `run_trial.py` defines trial-level flow.
- `util.py`/`utils.py` defines controller and helper logic.
- `config/config.yaml` defines task parameters.

Goal: produce a transparent and reproducible README that lets a researcher rebuild the task without opening source code.

IMPORTANT TABLE RULES (apply to all tables):
1. Always include a header row and separator row.
2. Leave one blank line after each table before the next paragraph/section.
3. Do not add extra table pipes or indentation.

# Task Name
Use the full task name as the top-level heading.

Meta table (no "Meta Information" header):
- Render a 2-column table (`Field`, `Value`).
- Use this exact header structure:
  | Field                | Value                        |
  |----------------------|------------------------------|
- Include rows:
  - Name
  - Version
  - URL / Repository
  - Short Description
  - Created By
  - Date Updated
  - PsyFlow Version
  - PsychoPy Version
  - Modality
  - Language
  - Voice Name
- Fill values when available; leave blank only if unknown.

## 1. Task Overview
One concise paragraph summarizing task goal and design.

## 2. Task Flow
Create separate tables for:
- Block-Level Flow (from `main.py`)
- Trial-Level Flow (from `run_trial.py`)
- Controller Logic (from `util.py` / `utils.py`, if present)
- Other Logic (if present in helper files; e.g., scoring, adaptive updates, side counterbalancing)

Descriptions must be implementation-detailed and reference timing/stimulus IDs where relevant.

## 3. Configuration Summary
State that settings are from `config/config.yaml`. Add these subtables:

a. Subject Info (`Field`, `Meaning`)
b. Window Settings (`Parameter`, `Value`)
c. Stimuli (`Name`, `Type`, `Description`)
d. Timing (`Phase`, `Duration`)
e. Triggers (`Event`, `Code`) when triggers exist
f. Adaptive Controller (`Parameter`, `Value`) when controller exists

## 4. Methods (for academic publication)
Write 2-3 academic paragraphs covering:
- Participant experience across phases
- Adaptive/controller logic and parameters (if any)
- Design rationale
- Concrete timing/setup details

IMPORTANT:
- Output only Markdown for `README.md`.
- Do not output explanations outside README content.
